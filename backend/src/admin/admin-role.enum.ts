/** Roles available to admin accounts. */
export enum AdminRole {
  /** Standard admin with limited write permissions. */
  ADMIN = 'ADMIN',
  /** Super-admin with full platform access. */
  SUPERADMIN = 'SUPERADMIN',
}
